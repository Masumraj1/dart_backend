# 🚀 Flutter Backend (Dart Shelf + MongoDB)

এটি একটি ডার্ট-বেসড ব্যাকএন্ড প্রোজেক্ট যা **Shelf** ফ্রেমওয়ার্ক এবং **MongoDB** ডাটাবেজ ব্যবহার করে তৈরি করা হয়েছে। এই সার্ভারটি মূলত ফ্লাটার অ্যাপের এন্ডপয়েন্ট হিসেবে কাজ করার জন্য ডিজাইন করা।

---


## 🛠 Prerequisites

এই প্রোজেক্টটি চালানোর জন্য আপনার পিসিতে নিচের টুলসগুলো ইনস্টল থাকতে হবে:
- [Dart SDK](https://dart.dev/get-dart)
- [MongoDB Community Edition](https://www.mongodb.com/docs/manual/administration/install-community/)

---

## 📥 Installation & Setup

### ১. MongoDB Setup (macOS)
প্রথমে আপনার পিসিতে MongoDB ইনস্টল এবং স্টার্ট করে নিন:

```bash
# ১. MongoDB Tap অ্যাড করুন
brew tap mongodb/brew

# ২. MongoDB ইনস্টল করুন
brew install mongodb-community@7.0

# ৩. সার্ভিস স্টার্ট করুন
brew services start mongodb-community@7.0
```

### ২. Project Setup
প্রোজেক্টটি ক্লোন করুন এবং ডিপেন্ডেন্সি ইনস্টল করুন:

```bash
# প্যাকেজ ইনস্টল করুন
dart pub get

# সার্ভার রান করুন
dart run lib/bin/server.dart
```

১. পাইপলাইন (Pipeline)
পাইপলাইন বলতে একটি ধারাবাহিক প্রক্রিয়া বোঝায়। যখন কোনো ক্লায়েন্ট সার্ভারে একটি রিকোয়েস্ট পাঠায়, সেটি সরাসরি ডাটাবেজে না গিয়ে কয়েকটি ধাপ বা "পাইপ"-এর মধ্য দিয়ে অতিক্রম করে।

২. মিডলওয়্যার (Middleware)
মিডলওয়্যার হলো পাইপলাইনের ভেতরের একেকটি চেকপোস্ট বা শর্ত। এটি রিকোয়েস্ট আপনার আসল লজিকে পৌঁছানোর আগে বা পরে নির্দিষ্ট কিছু কাজ সম্পাদন করে।

কেন আমরা এটি ব্যবহার করেছি?

🛡 Security: ইউজার অথেন্টিকেশন চেক করার জন্য।
📝 Logging: রিকোয়েস্টের ট্র্যাকিং রাখার জন্য (logRequests())।
📄 Data Formatting: রেসপন্সকে JSON ফরম্যাটে সাজিয়ে পাঠানোর জন্য (_jsonHeaderMiddleware)।
